{% extends 'base.html.twig' %}

{% block title %}Редактировать курс{% endblock %}

{% block body %}
    <h1>Редактировать курс</h1>

    {{ include('course/_form.html.twig', {'button_label': 'Обновить'}) }}

    <div class="d-flex flex-row gap-1 mt-2">
        <a class="btn btn-outline-secondary" href="{{ path('app_course_show', {'id': course.id}) }}">Отменить обновление</a>
        {{ include('course/_delete_form.html.twig') }}
    </div>
{% endblock %}
