{% extends 'base.html.twig' %}

{% block title %}Изменить урок | StudyOn{% endblock %}

{% block body %}
    <h1>Изменить урок</h1>

    {{ include('lesson/_form.html.twig', {'button_label': 'Обновить'}) }}

    <div class="d-flex flex-row gap-1 mt-2">
        <a class="btn btn-outline-secondary" href="{{ path('app_lesson_show', {'id': lesson.id}) }}">Отменить обновление</a>
        {{ include('lesson/_delete_form.html.twig') }}
    </div>
{% endblock %}
